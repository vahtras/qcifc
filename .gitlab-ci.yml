before_script: 
    - apt update -qy
    - apt install -y virtualenv python3-dev python3-pip gfortran libblas-dev liblapack-dev 
    - virtualenv venv3 -p python3
    - source venv3/bin/activate
    - pip install -r requirements.txt
    - pip install -r requirements-dev.txt

run:
    script:
        - make test
